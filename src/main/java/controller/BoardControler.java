package controller;

import model.AbstractModel;

public class BoardControler extends AbstractControler {
    public BoardControler(AbstractModel abstractModel) {
        super(abstractModel);
    }
}
